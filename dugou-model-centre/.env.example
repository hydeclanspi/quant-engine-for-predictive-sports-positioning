# DeepSeek — server-side only. Never prefix the API key with VITE_.
# Quick Input explicitly disables thinking mode for lower latency and token use.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-flash
DEEPSEEK_TIMEOUT_MS=12000
